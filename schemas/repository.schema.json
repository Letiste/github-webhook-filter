{
  "type": "object",
  "properties": {
    "repository": {
      "oneOf": [
        {
          "type": "object",
          "required": ["permittedActions"],
          "properties": {
            "permittedActions": {
              "description": "White listed actions",
              "$ref": "#/definitions/actionsAvailable"
            }
          }
        },
        {
          "type": "object",
          "required": ["filteredActions"],
          "properties": {
            "filteredActions": {
              "description": "Black listed actions",
              "$ref": "#/definitions/actionsAvailable"
            }
          }
        },
        {
          "type": "object",
          "required": ["allowedAll"],
          "properties": {
            "allowedAll": {
              "description": "If true, allow every actions",
              "type": "boolean"
            }
          }
        }
      ]
    }
  },
  "definitions": {
    "actionsAvailable": {
      "type": "array",
      "items": {
        "enum": ["created", "deleted", "archived", "unarchived", "edited", "renamed", "transferred", "publicized", "privatized"]
      },
      "uniqueItems": true
    }
  }
}
