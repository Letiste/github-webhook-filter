{
  "type": "object",
  "required": ["actions"],
  "properties": {
    "actions": {
      "type": "array",
      "description": "Permitted actions",
      "items": {
        "enum": ["created", "deleted"]
      },
      "uniqueItems": true
    }
  }
}
