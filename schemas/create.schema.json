{
  "type": "object",
  "properties": {
    "create": {
      "allOf": [
        {
          "oneOf": [
            {
              "type": "object",
              "required": ["permittedRefType"],
              "properties": {
                "permittedRefType": {
                  "description": "White listed RefType",
                  "$ref": "#/definitions/refTypeAvailable"
                }
              }
            },
            {
              "type": "object",
              "required": ["filteredRefType"],
              "properties": {
                "filteredRefType": {
                  "description": "Black listed RefType",
                  "$ref": "#/definitions/refTypeAvailable"
                }
              }
            },
            {
              "type": "object",
              "required": ["allowedAll"],
              "properties": {
                "allowedAll": {
                  "description": "If true, allow every RefType",
                  "type": "boolean"
                }
              }
            }
          ]
        },
        {
          "type": "object",
          "properties": { "users_black_listed": { "type": "array", "items": { "type": "string" } } }
        }
      ]
    }
  },
  "definitions": {
    "refTypeAvailable": {
      "type": "array",
      "items": {
        "enum": ["branch", "tag"]
      },
      "uniqueItems": true
    }
  }
}
